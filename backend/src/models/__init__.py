"""Data models: SQLModel tables (PostgreSQL) and Pydantic I/O schemas.

Note: table modules import SQLModel/SQLAlchemy; the Pydantic identifier schemas
in ``identifiers.py`` are dependency-light and safe to import anywhere.
"""
