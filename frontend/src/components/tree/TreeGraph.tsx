/**
 * TreeGraph — React Flow container for the recommendation provenance web.
 * Implemented in Phase 4 (React Flow `@xyflow/react` + D3 force layout).
 */
export function TreeGraph() {
  return null;
}
